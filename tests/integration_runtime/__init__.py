"""Runtime integration cases, grouped by area.

turns: plain turns and streaming/error recovery; sessions: config,
 prompt overlay, journal, steering and headless envelopes; budgets:
 token, cost and call budgets; memory: scope, redaction and the
 background extractor; recovery: timeouts, compaction and activity
 polls. Discovered through tests/integration.py.
"""
