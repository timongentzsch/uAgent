# Planted document

This file points at `docs/deliberately-missing.md`, which does not exist, so
stale_doc_path reports it.

A planted sentence long enough to trip the duplicate sentence detector across
two separate fixture documents.
